/* function showTodo(todo: { title: string; text: string }) {
  console.log(todo.title + ' ' + todo.text);
}

let myTodo = { title: 'Sun', text: 'The sun is shining' };

showTodo(myTodo); */
function showTodo(todo) {
    console.log(todo.title + ' ' + todo.text);
}
var myTodo = { title: 'Sun', text: 'The sun is shining' };
showTodo(myTodo);
